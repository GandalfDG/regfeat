extends layout

block content
  h1=title

  form(method='POST' action='')
    div.form-group
      label(for='feature_title') Title:
      input#feature_title.form-control(type='text', placeholder='Title' name='feature_title' required='true' value=(undefined===member ? '' : member.first_name) )
      label(for='last_name') Last Name:
      input#last_name.form-control(type='text', placeholder='Last name' name='last_name' required='true' value=(undefined===member ? '' : member.last_name))
      label(for='nickname') Nickname:
      input#nickname.form-control(type='text', placeholder='Nickname' name='nickname' value=(undefined===member ? '' : member.nickname))
    button.btn.btn-primary(type='submit') Submit
  if errors 
    ul
      for error in errors
        li!= error.msg